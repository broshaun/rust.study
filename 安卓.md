### 生成 keystore（macOS

keytool -genkeypair \
  -keystore chatly-release.jks \
  -storetype JKS \
  -alias chatly \
  -keyalg RSA -keysize 2048 -validity 10000


git tag android-v1.0.18
git push origin android-v1.0.18